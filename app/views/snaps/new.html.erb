<%= form_for @snap, :url => snaps_new_path, :html => { :multipart => true } do |f| %>
  <%= f.label "Image:" %> <br>
  <%= f.file_field :image %> <br>
  <%= f.label "Description:" %> <br>
  <%= f.text_area :description %> <br>
  <%= f.submit "Upload Snap" %>
<% end %>
